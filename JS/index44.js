let text = "HELLO";
console.log (text);
const firstChar = text.charAt(0); 
const secondChar = text.charAt(1); 
const thirdChar = text.charAt(2); 
const fourChar = text.charAt(3); 
const fifthChar = text.charAt(4); 

console.log ((fifthChar).concat (fourChar,thirdChar,secondChar,firstChar )); 
console.log ((fifthChar.toLowerCase()).concat (fourChar.toLowerCase(),thirdChar.toLowerCase(),secondChar.toLowerCase(),firstChar.toLowerCase() )); 
//const result =  + ;